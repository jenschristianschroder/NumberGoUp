# Required for worker in local dev (optional if not running worker locally)
SERVICE_BUS_CONNECTION_STRING=

# Local PostgreSQL (provided by docker-compose)
DATABASE_URL=postgresql://ngu_admin:localdev@localhost:5432/numbergoUp
DATABASE_SSL=false

# API
PORT=3000
LOG_LEVEL=debug
